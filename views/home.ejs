<%- include('partials/header'); %> 
  <% posts.forEach(function(post){ %>
    <h1><%= post.Title %></h1>
    <p><%= post.Content.substring(0,1000) %> 
      ... <a href="/posts/title/<%= post.Title %>">Read more</a>
    </p>
  <% }); %> 
<%- include('partials/footer'); %>
